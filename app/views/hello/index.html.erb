<h1>Hello, <%= @user_name %></h1>
<img src="<%= @picture %>">

<h2>Posts</h2>
<% @posts.each do |post| %>
  <div>
    <%= post.user.full_name %>
    <%= image_tag(post.user.picture_url) %>
    <%= post.content %>
  </div>
<% end %>
